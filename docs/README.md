# Docs

- [erd/](erd/) — entity relationship diagrams for the PrimeAutocare schema.
  - `erd-Conceptual.png` / `erd-Logical.png` — rendered diagrams.
  - `erd.drawio` — editable source, open with [draw.io](https://app.diagrams.net/).

  > These diagrams predate the July 2026 restructure (merged `JOBS` table,
  > dropped `vehicle_visit`/`job_assignment`, added `ATTENDANCE`) and have not
  > been re-rendered yet. Treat [../database/schema.sql](../database/schema.sql)
  > as the source of truth until they're updated.

For the database schema itself, see [../database/schema.sql](../database/schema.sql).
